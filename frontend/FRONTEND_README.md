# Pixel Patches Frontend

## Setup and run

1. Create a `.env` file with the following variables:
   ```
   BACKEND_URL="http://127.0.0.1:3000"
   ```

2. Start the server:
   ```bash
   npm start
   ```
